# Repeater bearer tokens — comma-separated name:token pairs.
# The name is for logging only; auth checks the token value.
# Generate tokens with: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Example: REPEATER_TOKENS=ctrl:abc123def456,agent-a:uvw987xyz321
REPEATER_TOKENS=