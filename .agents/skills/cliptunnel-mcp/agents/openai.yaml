interface:
  display_name: "Cliptunnel Mcp"
  short_description: "Repo-specific patterns and workflows for cliptunnel-mcp"
  default_prompt: "Use the cliptunnel-mcp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true